
<system>
    workers {{ fluentd_workers }}
</system>

# 共通設定

# ラベル: 内部のルーティング用に利用する。
# @to_cloud_watch_logs
#   CloudWatchLogsへの転送準備が出来たレコードに付けるラベル
#
# CloudWatchへの転送前に変換等が行いたい場合、変換用にラベルを設定して
# 変換後に"@to_cloud_watch_logs"を設定する

# 各種サービス用設定の読み込み
@include config.d/*.conf

